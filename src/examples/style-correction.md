---
{
  "styleGuide": "Use short, direct sentences without repetition."
}
---
# Style-focused Correction

Signal-kernel tracks reactive dependencies. Signal-kernel invalidates affected computations. Signal-kernel then schedules the affected computations so that the affected computations can update.
